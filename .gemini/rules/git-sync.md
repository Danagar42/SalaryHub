# Git Auto Sync Rule

After completing any task, code edit, or implementation step:
1. Stage all modified and new files (`git add .`).
2. Create a git commit with a clear, descriptive message in Ukrainian.
3. Push the changes to the remote GitHub repository (`git push`).
